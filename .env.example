# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
RAG_POSTGRES_PORT=15432
POSTGRES_DB=rag_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
RAG_API_KEY=change-me-in-production
RAG_BIND_ADDRESS=127.0.0.1

# Frontend/BFF deployment
# Keep RAG_API_KEY on the server-side proxy. Never expose it as a VITE_* variable.
RAG_API_UPSTREAM=http://host.docker.internal:8000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=16379
