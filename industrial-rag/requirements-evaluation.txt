# Isolated text-only judge stack. Regenerate with pip-tools 7.5.3 on Python 3.12:
# pip-compile --generate-hashes --no-emit-index-url --strip-extras requirements-evaluation.txt
# Keep this surface free of Ragas, LangChain, cache deserializers, URL loaders,
# and multimodal fetchers; the release audit enforces that invariant.
openai==3.6.0
