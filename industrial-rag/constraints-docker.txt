# Reproducible core runtime constraints for Linux Docker images.
# Torch itself is supplied by the digest-pinned CUDA base image (API) or the
# dedicated hash-locked CPU wheel (worker). requirements-runtime.lock.txt
# deliberately excludes the Torch distribution while retaining its resolved
# CPU dependency closure.
fastapi==0.139.2
starlette==1.3.1
prometheus-fastapi-instrumentator==8.0.0
torch==2.13.0
transformers==5.16.1
sentence-transformers==6.0.0
tokenizers==0.23.1
huggingface-hub==1.29.0
numpy==1.26.4
pandas==2.2.3
